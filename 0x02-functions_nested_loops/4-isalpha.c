#include <stdio.h>
#include "main.h"

/**
 * _isalpha - alphabetic character
 * @c: parameter
 *
 * Return: 0
 *
 *
 */

int isalpha(int);
int _isalpha(int c)
{
	if (c == isalpha(c))
	{
		return (1);
	}
	return (0);
}
